<!DOCTYPE html>
<html>
  <head>
    <title>SherpaApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Exo+2:wght@200;400;600&display=swap" rel="stylesheet">
  </head>

  <body>

    <header>
    <nav class="navbar navbar-expand-lg bg-dark">
    <div class= "container-fluid justify-content-start">
      <% if user_signed_in? %>
        <%=link_to "Log out", destroy_user_session_path, data: { turbo_method: :delete }, class: 'link px-2' %>
        Welcome <%= current_user.username %>!
        <%# OR USE button_to instead of link_to but turbo is a better solution %>
      <% else %>
        <%=link_to "Login", new_user_session_path, class: 'link px-2' %>
        <%= link_to "Sign up", new_user_registration_path, class: 'link px-2' %>
      <% end %>
      </div>
    </nav>
    <% if notice != "" && notice != nil %>
    <p class="alert alert-warning"><%= notice %></p>
    <% end %>
    <% if alert != "" && alert != nil %>
    <p class="alert alert-danger"><%= alert %></p>
    <% end %>

    </header>
  <div class = "container-fluid">
    <%= yield %>
  </div>
  <footer class="bg-dark">
  <p>By AZ-LCA March 2023 - Find me on Github and LinkedIn under the same @!</p>
  <footer/>
  </body>
</html>
